class_name: Samson
batch_size: 20
params:
  path_data_dir: "./data"
  H: 95
  W: 95
  n_endmembers: 3
  n_bands: 156
id: "${data.class_name}_bs${data.batch_size}_R${data.params.n_endmembers}_B${data.params.n_bands}"
